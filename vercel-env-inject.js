// Script para injetar variável de ambiente no HTML
// Este script será executado durante o build da Vercel
// Para usar, adicione no vercel.json ou configure como build command

// Este arquivo é apenas para referência
// A Vercel injeta variáveis de ambiente automaticamente no build
// Use o método de script inline no HTML ou configure via vercel.json

